#include <vector> // Include the necessary header for std::vector

using namespace std; // Import all names from the std namespace

int main() {
    vector<int> vec; // Declare vec as a variable of type std::vector<int>

    vec.push_back(4); // Use vec to push an element into the vector

    return 0;
}